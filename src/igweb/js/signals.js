
// Signals. Memory-inefficient one-time notification system. Hopefully
// we don't have too many of them; I am too lazy to make this more
// efficient than it already is.
//
var Signal = function(isConstant) {
  
};

Signal.prototype.dependOn = function(signal) {

};

Signal.prototype.register = function(callback) {
                              
};

Signal.prototype.set = function() {
    
};

Signal.prototype.undependOn = function(signal) {
  if (this.dependencies) {
    
  }
};
